<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <application
        android:name=".UnicityWalletApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UnicityWallet">
        <activity
            android:name=".ui.launcher.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.UnicityWallet">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.onboarding.welcome.WelcomeActivity"
            android:exported="false"
            android:label="Welcome"
            android:theme="@style/Theme.UnicityWallet"
            android:configChanges="orientation|screenSize|keyboardHidden"
            />
        <activity
            android:name=".ui.onboarding.createNameTag.CreateNameTagActivity"
            android:exported="false"
            android:label="Create Nametag"
            android:theme="@style/Theme.UnicityWallet"
            android:configChanges="orientation|screenSize|keyboardHidden"
            />
        <activity
            android:name=".ui.wallet.WalletActivity"
            android:exported="false"
            android:label="Wallet"
            android:theme="@style/Theme.UnicityWallet"
            android:configChanges="orientation|screenSize|keyboardHidden"
            />
    </application>

</manifest>